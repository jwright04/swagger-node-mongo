let chai    = require('chai');
let expect  = chai.expect;
let express = require('express');
let Users   = require('../../../api/models/usersModel');
let app     = express();

/*
 describe("models", () => {
 describe("users", () => {
 describe("compare user model", () => {
 it("should contain all fields from users model", (done) => {
 let users = new Users();

 });
 });
 });
 });*/
